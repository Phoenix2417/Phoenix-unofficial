tải file về, sử dụng npm i để down packages ( nếu cmd vẫn báo lỗi ko tìm thấy packages nào thì npm i package đó )
nhớ thay id ở 1 số mdl và trong config
lấy cookie acc bot và gắn vô file cookie.txt 
$lưu ý: tải file bot về nhớ xóa file hướng dẫn 
